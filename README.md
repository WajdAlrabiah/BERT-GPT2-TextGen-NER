# BERT-GPT2-TextGen-NER
A simple NLP lab using Hugging Face Transformers that demonstrates text generation with GPT-2 and Named Entity Recognition (NER) with BERT. The project allows user interaction for custom input and displays generated outputs and detected entities with confidence scores.
